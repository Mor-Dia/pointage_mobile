name: user_repository
description: Dart package which manages the user domain.
publish_to: none

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  equatable: ^2.0.0
  uuid: ^3.0.0
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.0